let length = prompt("What is the length of the rectangle?");
let width = prompt("What is the width of the rectangle?");

let area = width * length;

alert(`The area of the rectangle you provided is ${area} square units`)
